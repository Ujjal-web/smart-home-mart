export default function Footer() {
    return (
        <footer>
            <div className="wrap">
                <small>© {new Date().getFullYear()} SmartHome Marketplace</small>
            </div>
        </footer>
    );
}